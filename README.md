# cler.su — клиент

Статический фронтенд (GitHub Pages / любой CDN).

## Сервер

Прод: `wss://ffa.agar.su:6010`.

URL задаётся в `js/config/servers.js`.

## Локальный просмотр

Любой static server из корня этой папки, например:

```bash
npx --yes serve .
```

Или просто залейте содержимое репозитория на GitHub Pages.
